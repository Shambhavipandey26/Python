import array

arr = array.array('i', [1, 2, 3,4, 5, 6])

a=eval(input("Enter the value to be searched:"))

for i in range(len(arr)):
    if a==arr[i]:
      if a%2==0 :
          print ("Element is even")
      else:
          print ("Element is odd")

